// =============================================================================
// TEMPLATE: domain/repositories/{prefix}_repository.dart
// Replace {prefix}, {Prefix}, {Feature} with your module values.
// =============================================================================

// TODO: Import all entity types used in the contract
// import 'package:majadigi_mobile_rebuild/deferred/{module_name}/domain/entities/{feature}/{prefix}_{feature}_entity.dart';

/// Represent contracts for this module's data operations.
/// Implementation lives in data/repositories/{prefix}_repository_impl.dart
// abstract class {Prefix}Repository {
//   /// Fetch all {feature} items (returns cached, fires background sync)
//   Future<List<{Prefix}{Feature}Entity>> get{Feature}();
//   Stream<List<{Prefix}{Feature}Entity>> watch{Feature}();
//
//   /// Sync {feature} from remote API to local Isar cache
//   Future<void> sync{Feature}();
//
//   // TODO: Add more get/sync/search methods per feature
// }
